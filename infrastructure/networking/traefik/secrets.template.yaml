# Create this secret in your overlay using SealedSecrets
# Generate password with: htpasswd -nb admin <password>

apiVersion: v1
kind: Secret
metadata:
  name: traefik-dashboard-auth
  namespace: traefik
type: Opaque
stringData:
  users: |
    admin:$apr1$...generated...hash...
