apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-operator
  namespace: tailscale
spec:
  interval: 30m
  install:
    timeout: 10m
  upgrade:
    timeout: 10m
  chart:
    spec:
      chart: tailscale-operator
      version: "1.102.2"
      sourceRef:
        kind: HelmRepository
        name: tailscale-operator
      interval: 12h
