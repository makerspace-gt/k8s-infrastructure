apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: discord
  namespace: flux-system
spec:
  type: discord
  # Webhook URL lives in the `address` key of this sealed secret (see sealed-secret.yaml).
  secretRef:
    name: flux-discord-webhook
