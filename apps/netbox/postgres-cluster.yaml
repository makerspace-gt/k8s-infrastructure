apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: netbox-db
  namespace: netbox
spec:
  instances: 2
  storage:
    size: 10Gi
  bootstrap:
    initdb:
      database: netbox
      owner: netbox
