apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vaultwarden
  namespace: vaultwarden
  annotations:
    cert-manager.io/cluster-issuer: staging-ca-issuer
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - vault.staging.local
      secretName: vaultwarden-tls
  rules:
    - host: vault.staging.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vaultwarden
                port:
                  number: 80
