apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: zammad
  namespace: zammad
spec:
  interval: 30m
  chart:
    spec:
      chart: zammad
      version: ">=11.0.0"
      sourceRef:
        kind: HelmRepository
        name: zammad
      interval: 12h
  install:
    timeout: 15m
  upgrade:
    timeout: 15m
  values:
    # Disable built-in PostgreSQL
    postgresql:
      enabled: false

    # Use external PostgreSQL (CloudNativePG)
    zammadConfig:
      postgresql:
        host: "zammad-db-rw.zammad.svc.cluster.local"
        port: 5432
        db: "zammad"
        user: "zammad"

    # Point to CloudNativePG secret for password
    envFromSecret: zammad-db-app

    # Enable Elasticsearch
    elasticsearch:
      enabled: true
      replicas: 1

    # Enable Redis
    redis:
      enabled: true

    # Enable Memcached
    memcached:
      enabled: true

    # Ingress
    ingress:
      enabled: true
      className: traefik
      hosts:
        - host: zammad.localhost
          paths:
            - path: /
              pathType: Prefix
