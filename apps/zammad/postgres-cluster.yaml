apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: zammad-db
  namespace: zammad
spec:
  instances: 2
  storage:
    size: 20Gi
  bootstrap:
    initdb:
      database: zammad
      owner: zammad
